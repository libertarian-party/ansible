---
- name: Install iptables-persistent
  apt:
    name: iptables-persistent

- name: Create directory for iptables configuration
  file:
    state: directory
    path: '{{ security__conf_dir }}'
    mode: 'u=rwx,g=rx,o=rx'
    owner: root
    group: root

- name: Install iptables rules for IPv4
  template:
    src: templates/rules.v4
    dest: '{{ security__conf_ipv4 }}'
    mode: 'u=rw,g=r,o=r'
    owner: root
    group: root
  notify: security | Load rules for IPv4

- name: Install iptables rules for IPv6
  template:
    src: templates/rules.v6
    dest: '{{ security__conf_ipv6 }}'
    mode: 'u=rw,g=r,o=r'
    owner: root
    group: root
  notify: security | Load rules for IPv6
